const configLink = "/icons";

export const iconConfig: Record<string, string> = Object.freeze({
    'phone-call': `${configLink}/phone.svg`,
    'check': `${configLink}/check.svg`,
    'il': `${configLink}/il.svg`,
    'fr': `${configLink}/fr.svg`,
    'us': `${configLink}/us.svg`,
    'ru': `${configLink}/ru.svg`,

    // contries icons
});