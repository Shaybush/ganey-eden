export interface navbarContriesModel {
    icon: string,
    alt?: string,
    id: string,
    locale: string;
}

export interface navbarLinksModel {
    label: string,
    link: string,
    externalRoute: boolean;
    id: string;
}